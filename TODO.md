# TODO

- Test the skills in Codex. Install path and `$ARGUMENTS` should carry over
  from Claude Code, but this has not been run in Codex yet. `review-output`
  needs the most attention: on Codex the independent review falls back to a
  separate `codex exec` call instead of a spawned sub-agent.
